#pragma once
#include <iostream>

class Operaciones{
	public:
		Operaciones()=default;
		~Operaciones();
		void encerar(int []);
		void ingresar(int []);
		float procesar(int []);
		void imprimir(int []);
};
